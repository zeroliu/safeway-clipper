{
  "name": "safeway-coupons-clipper",
  "description": "This is a Safeway coupons clipper actor that demonstrates how to use the crawlee library to scrape and clip coupons from the Safeway website.",
  "version": "0.0.1",
  "author": "shadowwalker",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "npm run start:dev",
    "start:prod": "NODE_ENV=production node dist/main.js",
    "start:dev": "tsx src/main.ts",
    "build": "tsc",
    "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.9.2",
    "crawlee": "^3.9.1",
    "dotenv": "^16.4.5",
    "playwright": "*",
    "typescript": "~5.4.5",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@apify/tsconfig": "^0.1.0",
    "@types/node": "^20.12.7",
    "tsx": "^4.7.2"
  }
}
